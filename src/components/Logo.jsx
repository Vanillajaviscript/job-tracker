import logo from '../assets/images/logo.svg';

const Logo = () => {
  return( 
  <div>
    <h4>Job Tracker <img src={logo} alt="Job Tracker" className="logo" /></h4>
  </div>
  )
}

export default Logo